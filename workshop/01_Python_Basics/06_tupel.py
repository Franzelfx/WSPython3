"""06 · Tupel

Tupel sind unveränderbar. Ideal für feste Datensätze.

Übung: Speichere eine RGB-Farbe als Tupel.
"""

point = (3, 4)
x, y = point
print(x, y)
# point[0] = 5  # Error!

# Übung: Speichere eine RGB-Farbe als Tupel
color = (255, 0, 0)
r, g, b = color
print(r, g, b)